function main(numsToPrint, numsToAdd) {

    let sequence = [1];
    let sum = 1;

    for (let i = 1; i < numsToPrint; i++) {
        let start = Math.max(0, i - k);
        let end = i - 1;
    }

}

main(6, 3);
main();
main();
   